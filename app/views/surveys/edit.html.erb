<div class="left-sidebar">    
	<div class="row-fluid">
		<div class="span12">
			<div class="widget">
				<div class="widget-header">
					<div class="title">
						Edit <%= @survey.survey_name %>
						<span class="mini-title" style="display:none">
							Editing Survey
						</span>
					</div>
				</div>
				<div class="widget-body">
					<%= simple_form_for(@survey, :html => {:class => 'form-horizontal' }) do |f| %>
					<%= f.input :survey_name, placeholder: 'Enter Survey Name' %>
					<%= f.simple_fields_for :questions do |builder| %>
					<%= render 'question_fields', f: builder %>
					<% end %>
					<%= link_to_add_fields "Add Question", f, :questions %>
					<div class="form-actions no-margin">
						<button type="submit" class="btn btn-info pull-right">
							Update Survey
						</button>
						<div class="clearfix">
						</div>
					</div>
					<% end %>
				</div>
			</div>
		</div>
	</div>
</div>