<div class="right-sidebar">
	<div class="wrapper">
		<div id="scrollbar">
			<div class="scrollbar">
				<div class="track">
					<div class="thumb">
						<div class="end">
						</div>
					</div>
				</div>
			</div>
			<div class="">
				<div class="overview">
					<div class="featured-articles-container">
						<h5 class="heading-blue">
							<%= 'Manage User' if params[:controller] == 'users' %>
							<%= 'Manage Employee' if params[:controller] == 'employees' %>
							<%= 'Manage Student' if params[:controller] == 'students' %>
						</h5>
						<div class="articles">
							<% if params[:controller] == 'users' %>
							<a href="<%= new_user_path %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Add New Admin
							</a>
							<a href="<%= users_path(tab: 'active') %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Active Users
							</a>
							<a href="<%= users_path(tab: 'inactive') %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Inactive Users
							</a>
							<% end %>
							<% if params[:controller] == 'employees'%>
							<a href="<%= new_employee_path %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Add New Employee
							</a>
							<a href="<%= employees_path(tab: 'staff_not_users') %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Add Staff As Users
							</a>
							<a href="<%= employees_path(tab: 'advisors_not_users') %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Add Advisors As Users
							</a>
							<% end %>
							<% if params[:controller] == 'students' %>
							<a href="<%= new_student_path %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Add New Student
							</a>
							
							<a href="<%= students_path(tab: 'not_users') %>">
								<span class="label-bullet-blue">
									&nbsp;
								</span>
								Add Students As Users
							</a>
							<% end %>
							
						</div>
					</div>

				</div>
			</div>
		</div>
	</div>
</div>
