<% if @subject.subject_surveys.empty? || @subject.subject_surveys.nil? %>
toastr.error('Sorry, no one answered this survey.. no reports yet!')
<% else %>
window.location.href = '<%= survey_subject_path(@subject.survey_id,@subject.id) %>'
<% end %>